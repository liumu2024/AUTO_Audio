/** 与 shared/types/pipeline 对齐 — 前端 Pipeline 类型 */
export type {
  GenerationResult,
  OutlineSegment,
  PipelineBundle,
  TaskProgressEvent,
  TaskStatus,
  UserMaterialDto,
  VideoIngest,
} from '@shared/types/pipeline'

export { TASK_STATUS } from '@shared/types/pipeline'
